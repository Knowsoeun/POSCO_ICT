<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<insert id="insert" parameterType="BoardDTO" useGeneratedKeys="true" keyProperty="board_id">
		<![CDATA[
		INSERT INTO board (title, content, user) VALUES (#{title}, #{content}, #{user})
		]]>
	</insert>
	<select id="select_detail" parameterType="BoardDTO" resultType="BoardDTO">
		<![CDATA[
		SELECT title, content, user, insert_date FROM board WHERE board_id = #{board_id};
		]]>
	</select>
	<update id="updateById" parameterType="BoardDTO">
	
		<![CDATA[
		 update board set title = #{title}, content = #{content}, user = #{user} where board_id = #{board_id}
		]]>
	</update>
	<delete id="deleteById" parameterType="BoardDTO">
	
		<![CDATA[
		 delete from board where board_id = #{board_id}
		]]>
	</delete>
	
	<select id="selectList" parameterType="BoardDTO" resultType="BoardDTO">
		<![CDATA[
		SELECT board_id, title, content, user, insert_date FROM board ;
		]]>
	</select>
	
	
</mapper>