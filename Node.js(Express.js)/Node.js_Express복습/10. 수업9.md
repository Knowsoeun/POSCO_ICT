## app.js
```js
var express = require('express');
const { listenerCount } = require('process');

// 터미널에서 npm install express 실행하기

var app = express();

app.get('/', function(req, res){
    console.log('/ 접속 성공');
    
    res.writeHead(200, {'content-Type': "text/html; charset=utf-8"});
    res.write("<!DOCTYPE html>");
    res.write("<html>");

    res.write("<head>");
    res.write("<title> 응답페이지</title>");
    res.write("</head>");
    res.write("<body>");
    res.write("<h1>Node.js로부터 응답 HTML 코드</h1>");
    res.write("</body>");
    res.write("</html>");

    //res.end('hi hello');  // send --> 데이터를 보내준다.
    //res.send('hi hello');
});

// server create + listener 
var listen = app.listen(8090, function(){
    console.log("server start port:"+listenerCount.address().port);
}); 

/* app.get('/test', (req, res)=> res.send('Hello Express Server'))

app.listen(8090, ()=>console.log('server start...m')); */
```
